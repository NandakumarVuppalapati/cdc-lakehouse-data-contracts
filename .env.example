# Copy to .env and adjust if needed. Compose reads this automatically.
POSTGRES_USER=lakehouse
POSTGRES_PASSWORD=lakehouse
POSTGRES_DB=sourcedb
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
