# Auto-provisioned Prometheus datasource — no manual "Add data source"
# click-through needed on first boot. Grafana reads every *.yml under
# provisioning/datasources/ on startup.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
