{
  "_deprecated": true,
  "_reason": "Superseded by iceberg-sink-customers.json, iceberg-sink-products.json, iceberg-sink-orders.json, iceberg-sink-order-items.json. This single-connector, multi-topic-to-multi-table config was never actually a supported pattern for io.tabular.iceberg.connect.IcebergSinkConnector without a route-field — it caused real record misrouting between tables (confirmed: bronze.orders ended up with the order_items schema). See ADR 0012. Not deleted only because this Windows-synced folder doesn't allow file deletion from the sandbox; this file is inert and unused by scripts/register-connectors.sh."
}
